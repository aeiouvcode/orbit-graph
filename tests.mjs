import fs from 'node:fs';const h=fs.readFileSync('index.html','utf8'),j=fs.readFileSync('app.js','utf8');for(const s of ['mapView','world','edges','inspector','importFile'])if(!h.includes(`id="${s}"`))throw Error('missing '+s);for(const s of ['localStorage','pointerdown','wheel','JSON.stringify','JSON.parse'])if(!j.includes(s))throw Error('missing '+s);new Function(j);console.log('PASS static structure, feature hooks, and JavaScript syntax');
